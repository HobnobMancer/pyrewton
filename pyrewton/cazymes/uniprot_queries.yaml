# This is the configuration file for quering UniProtKB
# Gather queries into groups (the first level)
# Then write out each query (indented under its group title), identified
# by starting the line with 'query:'
# Add additionally comments by starting the line with '#'
ec_queries:
  query:"ec:3.2.1.1"
  query:"ec:3.2.1.5"
cazy_query:
  query:"database:(type:cazy)"
